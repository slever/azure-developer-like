app.title=User Feedback(local)
# Enable related features.

## Azure Active Directory
# Enable or Disable AAD Login
spring.cloud.azure.active-directory.enabled=true
# Specifies your Active Directory ID:
spring.cloud.azure.active-directory.profile.tenant-id=KEYVAULT
# Specifies your App Registration's Application ID:
spring.cloud.azure.active-directory.credential.client-id=KEYVAULT
# Specifies your App Registration's secret key:
spring.cloud.azure.active-directory.credential.client-secret=KEYVAULT

## Cosmos DB
# Specify the DNS URI of your Azure Cosmos DB.
azure.cosmos.uri=KEYVAULT
# Specify the access key for your database.
azure.cosmos.key=KEYVAULT
# Specify the name of your database.
azure.cosmos.database=KEYVAULT
azure.cosmos.populateQueryMetrics=false